# Longest Subarray With Sum ≤ K
## Problem:

- Given an array of positive integers and integer k,
- find the length of the longest subarray whose sum is ≤ k.

## Example:
- a = [2,5,1,7,10]
- k = 8
- Answer = 3  (subarray = [2,5,1])
